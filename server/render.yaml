services:
  - type: web
    name: rhythm-flow-server
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
    domains:
      - rhythm-flow-server.onrender.com